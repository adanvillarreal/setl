program aPlusB;

int aa1a1a1a1a1a1a1a, a, b;

bool t, y;

int sum(){
     int res;
     a := 5 * 4 + 1 + 2 * 3;
     a := res + a;
     b := a + 1;
     res := a + b;
     return res;
}

main {
     int a, y;
     read(a, b);

     if(a + b > y) {
      a := 5;
     } else {
      a := 10;
     }

     if(a > b) {
      a := 999;
     }

     while( a != b && 3 > 5){
      while(true){

      }
     }
     a := 1;
     print(sum(a, b));
}
