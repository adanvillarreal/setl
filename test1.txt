program fibonacci;

set<int> allNumbersLessThan(int x) {
     int i;
     set<int> numbers;

     i := 1;
     while(i <= (x - 4)) {
          numbers.insert(i);
          i := i + 1;
     }
     return numbers;
}


main {
     int f1, f2, f3;
     int limit;
     int cont;
     set<int> fibonacciNumbers;
     set<int> allNumbers;

     f1 := 0;
     f2 := 1;
     f3 := f1 + f2;
     read(limit);

     allNumbers := allNumbersLessThan(limit);
     something(123, "a", allNumbers);

     print(true == false);

     while(f3 <= limit) {
          fibonacciNumbers.insert(f3);
          print(f3);
          print("");
          f1 := f2;
          f2 := f3;
          f3 := f1 + f2;
     }
     if(a()) {
       if(3 < 4 && 5 > 9) {
         p := 3;
       } else {
         print("nothing", 123);
       }
     } else {
        if(3) {
          x := 4;
        }
        else {
          opsdf("asdasd", a(fa("asd", 123, 543) - b.size()));
        }
     }

     allNumbers := allNumbers - fibonacciNumbers;
     print(allNumbers);

}
